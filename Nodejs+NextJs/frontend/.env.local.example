# Backend URL used by next.config.js rewrites (server-side only, NOT exposed to browser)
# In production set this to http://localhost:5000 (or wherever the backend runs)
BACKEND_URL=http://localhost:5000
